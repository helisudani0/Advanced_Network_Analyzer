[package]
name = "ravynel-agent-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Rust core for Ravynel distributed network sensor agents"

[lib]
name = "ravynel_agent_core"
path = "src/lib.rs"
